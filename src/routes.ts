export const ROUTES = {
  LOGIN: '/login',
  REGISTRATION: '/registration',
  HOME: '/',
  DASHBOARD: '/dashboard',
}

export const PORT = 3000;
export const LOCAL_ORIGIN = `http://localhost:${PORT}`;
